<UserControl x:Class="SPT_AKI_Profile_Editor.SettingsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SPT_AKI_Profile_Editor" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             Dialog:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0">
            <Label.Content>
                <TextBlock TextWrapping="Wrap" Text="{Binding AppLocalization.Translations[tab_settings_lang]}"/>
            </Label.Content>
        </Label>
        <ComboBox Grid.Column="2" Grid.Row="0" Margin="10,5,10,0" VerticalAlignment="Center" ItemsSource="{Binding LocalizationsList}" SelectedValue="{Binding CurrentLocalization}" SelectedValuePath="Key" DisplayMemberPath="Value"/>
        <Label Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0">
            <Label.Content>
                <TextBlock TextWrapping="Wrap" Text="{Binding AppLocalization.Translations[tab_settings_server]}"/>
            </Label.Content>
        </Label>
        <Label Grid.Column="1" Grid.Row="1" Foreground="Red" Visibility="{Binding InvalidServerLocationIcon}" ToolTip="{Binding AppLocalization.Translations[invalid_server_location_text]}" VerticalAlignment="Center" HorizontalAlignment="Center">
            <iconPacks:PackIconFontAwesome Kind="ExclamationTriangleSolid" Width="24" Height="24"/>
        </Label>
        <Grid Grid.Column="2" Grid.Row="1" VerticalAlignment="Center" Margin="10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" TextWrapping="Wrap" Text="{Binding ServerPath}" IsEnabled="False"/>
            <Button Grid.Column="1" Content="{Binding AppLocalization.Translations[button_select]}" Command="{Binding ServerSelect}" Margin="5,0,0,0"/>
        </Grid>
        <Label Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0">
            <Label.Content>
                <TextBlock TextWrapping="Wrap" Text="{Binding AppLocalization.Translations[tab_settings_account]}"/>
            </Label.Content>
        </Label>
        <Label Grid.Column="1" Grid.Row="2" Foreground="Red" Visibility="{Binding NoAccountsIcon}" ToolTip="{Binding AppLocalization.Translations[no_accounts]}" VerticalAlignment="Center" HorizontalAlignment="Center">
            <iconPacks:PackIconFontAwesome Kind="ExclamationTriangleSolid" Width="24" Height="24"/>
        </Label>
        <ComboBox Grid.Column="2" Grid.Row="2" Margin="10,5,10,0" VerticalAlignment="Center" ItemsSource="{Binding AppSettings.ServerProfiles}" SelectedValuePath="Key" DisplayMemberPath="Value" SelectedValue="{Binding AppSettings.DefaultProfile}" IsEnabled="{Binding AccountsBoxEnabled}"/>
        <Label Grid.Column="0" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0">
            <Label.Content>
                <TextBlock TextWrapping="Wrap" Text="{Binding AppLocalization.Translations[tab_settings_color_scheme]}"/>
            </Label.Content>
        </Label>
        <ComboBox Grid.Column="2" Grid.Row="3" Margin="10,5,10,0" VerticalAlignment="Center" ItemsSource="{Binding ColorSchemes}" SelectedValuePath="Scheme" SelectedValue="{Binding ColorScheme}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="30" Height="15" Fill="{Binding Path=Color}"/>
                        <Label Content="{Binding Path=Name}" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <StackPanel Grid.Row="4"  Grid.ColumnSpan="3" Orientation="Horizontal" MinHeight="85" HorizontalAlignment="Right">
            <Button Margin="0 0 5 0" Style="{DynamicResource MahApps.Styles.Button.Dialogs}" Content="{Binding AppLocalization.Translations[button_quit]}" Command="{Binding QuitCommand}" Visibility="Visible"/>
            <Button Margin="5 0 5 0" Style="{DynamicResource MahApps.Styles.Button.Dialogs.Accent}" Content="{Binding AppLocalization.Translations[button_close]}" Command="{Binding CloseCommand}" Visibility="{Binding CloseButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
