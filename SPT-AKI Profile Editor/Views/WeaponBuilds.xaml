<UserControl x:Class="SPT_AKI_Profile_Editor.Views.WeaponBuilds"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:SPT_AKI_Profile_Editor.Views"
             xmlns:helpers="clr-namespace:SPT_AKI_Profile_Editor.Helpers"
             d:DataContext="{d:DesignInstance Type=views:WeaponBuildsViewModel}"
             Dialog:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding Profile.WBuilds}">
            <ListBox.Resources>
                <helpers:BindingProxy x:Key="proxy" Data="{Binding}" />
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}" BorderThickness="1" Margin="5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="{Binding Path=Key}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontWeight="Bold" FontSize="14" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <Label Content="{Binding Data.AppLocalization.Translations[tab_mastering_weapon], Source={StaticResource proxy}}" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <Label Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Label.Content>
                                    <TextBlock TextWrapping="Wrap" Text="{Binding Path=Value.Weapon}"/>
                                </Label.Content>
                            </Label>
                            <Button x:Name="PresetExport" Content="{Binding Data.AppLocalization.Translations[tab_presets_export], Source={StaticResource proxy}}" Grid.Row="0" Grid.Column="2" Margin="5" Command="{Binding Data.ExportBuild, Source={StaticResource proxy}}" CommandParameter="{Binding Value}"/>
                            <Button x:Name="PresetRemove" Content="{Binding Data.AppLocalization.Translations[tab_backups_remove], Source={StaticResource proxy}}" Grid.Row="1" Grid.Column="2" Margin="5" Command="{Binding Data.RemoveBuild, Source={StaticResource proxy}}" CommandParameter="{Binding Key}"/>
                            <Label Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Label.Content>
                                    <TextBlock TextWrapping="Wrap" Text="{Binding Data.AppLocalization.Translations[tab_presets_ergonomics], Source={StaticResource proxy}}"/>
                                </Label.Content>
                            </Label>
                            <Label Content="{Binding Path=Value.Ergonomics}" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <Label Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Label.Content>
                                    <TextBlock TextWrapping="Wrap" Text="{Binding Data.AppLocalization.Translations[tab_preset_recoil_up], Source={StaticResource proxy}}"/>
                                </Label.Content>
                            </Label>
                            <Label Content="{Binding Path=Value.RecoilForceUp}" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <Label Grid.Row="4" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Label.Content>
                                    <TextBlock TextWrapping="Wrap" Text="{Binding Data.AppLocalization.Translations[tab_preset_recoil_back], Source={StaticResource proxy}}"/>
                                </Label.Content>
                            </Label>
                            <Label Content="{Binding Path=Value.RecoilForceBack}" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Row="1" Content="{Binding AppLocalization.Translations[tab_presets_import]}" Width="200" Margin="5,10,5,0" Command="{Binding ImportBuilds}"/>
    </Grid>
</UserControl>
